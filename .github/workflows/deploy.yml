name: Deploy React App

on:
  push:
    branch:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
     - name: Checkout code
       uses: actions/checkout@v2

     - name: Install dependencies 
       run : npm install

     - name: Set username and id
       run: |
       git config --global user.email "tamilmani793@gmail.com"
       git config --global user.name "Tamilmani"  

     - name: Build and Deploy
       run: |
            npm run build
            npm run deploy  